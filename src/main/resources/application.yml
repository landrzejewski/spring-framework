spring:
  datasource:
    username: admin
    password: admin
    url: jdbc:postgresql://localhost:5432/payments
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  messages:
    basename: messages,exceptions
  security:
      oauth2:
        resourceserver:
          jwt:
            issuer-uri: http://localhost:8100/realms/training
        client:
          registration:
            github:
              client-id: Ov23liiFhjiNjU0BtQrk
              client-secret: 44e9c6c3b3ea348d5c0fb5589070c50a6945e740
            keycloak:
              client-id: training-client
              scope:
                - openid
          provider:
            keycloak:
              issuer-uri: http://localhost:8100/realms/training
#  security:
#    user:
#      name: jan
#      password: 123
#      roles: ADMIN
logging:
  level:
    org.springframework.security: DEBUG
